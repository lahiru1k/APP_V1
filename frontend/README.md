# Frontend

Static landing page for the Serendip Match starter.

## Run locally

```bash
python -m http.server 8001
```

Open `http://localhost:8001` in your browser. Run the backend to see the live match preview.
